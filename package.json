{
  "name": "sdk-monorepo-root",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/demo",
    "build:sdk": "npm run build --workspace=packages/sdk"
  }
}
